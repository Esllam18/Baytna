# Database

Sprint 16 uses **Alembic** as the authoritative migration mechanism.

Migration history:
`backend/alembic/versions/`

Do not add raw SQL migrations to this folder unless a migration explicitly requires a database-specific operation.
